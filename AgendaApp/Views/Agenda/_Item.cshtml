@model ItemVM

<a class="list-group-item list-group-item-action flex-column align-items-start mb-1">
    <div class="d-flex w-100 justify-content-between">
        <h5 class="mb-1">@Model.Description</h5>
        <small>@Model.CreatedAt.ToString("d MMMM, yyyy")</small>
    </div>
    <p class="mb-1">Category: @Model.Category</p>
</a>